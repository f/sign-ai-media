{
  "name": "sign-ai-media-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:cdn": "tsc --noEmit && vite build --config vite.browser.config.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  },
  "dependencies": {
    "@contentauth/c2pa-web": "^0.8.0"
  }
}
